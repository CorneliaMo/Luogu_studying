#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Marquest.h"
#include <time.h>

int * RadixSort(int *a, int n) //基数排序(RadixSort)
/*BinSort想法非常简单，首先创建数组A[MaxValue]；
然后将每个数放到相应的位置上（例如17放在下标17的数组位置）；
最后遍历数组，即为排序后的结果。*/
{

}